<div class="container-wrap">
<div class="container">
  
<p id="notice"><%= notice %></p>

<h1>List of Problems</h1>
<%= link_to 'New Problem', new_problem_path, :class => 'btn btn-primary' %>

<table class="problems-list-table table">
  <thead>
    <tr>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @problems.each do |problem| %>
      <tr>
        <td><%= problem.title %></td>
        <td><%= link_to 'View', problem, :class => 'btn btn-default' %></td>
        <td><%= link_to 'Edit', edit_problem_path(problem), :class => 'btn btn-primary' %></td>
        <td><%= link_to 'Delete', problem, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

</div>
</div>