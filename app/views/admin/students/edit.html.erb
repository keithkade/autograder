<% content_for :title do %>
  Editing <%= @student.Name %>
<% end %>

<% content_for :actions do %>
  <%= link_to 'Cancel', admin_student_path(@student), :class => 'btn btn-danger' %>
<% end %>

<%= render 'form' %>