<% content_for :title do %>
  Editing <%= @course.name %>
<% end %>

<% content_for :options do %>
  <%= link_to 'Cancel', admin_courses_path(@course), :class => 'btn btn-danger' %>
<% end %>

<%= render 'form' %>