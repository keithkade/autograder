<% content_for :title do %>
  Editing <%= @course.name %>
<% end %>

<%= render 'form' %>

<% content_for :options do %>
  <%= link_to 'Cancel', admin_course_path(@course), :class => 'btn btn-danger' %>
<% end %>